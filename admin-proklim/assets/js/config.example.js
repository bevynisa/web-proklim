/* Salin file ini menjadi "config.js" (di folder yang sama), lalu isi dua
   nilai di bawah dengan milik project Supabase Anda sendiri.
   Lihat Project Settings > API di dashboard Supabase.

   Kedua nilai ini AMAN dipakai di kode sisi browser (bukan rahasia) —
   perlindungan datanya ada di aturan Row Level Security di database,
   bukan di kerahasiaan anon key ini. */
window.PROKLIM_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: "",
};
